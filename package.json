{
  "name": "nestflow-server",
  "version": "4.1.0",
  "description": "NestFlow nesting server — worker process per job",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "clipper-lib": "^6.4.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
